# FAERS-FAIR — Python dependency manifest
# Verified versions match the .venv used for the 2026-05-07 submission build.
# Re-create the venv with:
#     python3.12 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Core data stack
pandas==2.3.3
numpy==2.3.5

# Plotting
matplotlib==3.10.0
seaborn==0.13.2
missingno==0.5.2
pillow==12.2.0
fonttools==4.62.1

# Graph analytics
networkx==3.6.1
python-louvain==0.16

# Notebooks
jupyter==1.1.1
ipykernel==7.2.0
jupyterlab==4.5.7

# PDF generation (pure-Python fallback used because no LaTeX engine)
markdown_pdf==1.13.1
markdown-it-py==3.0.0
pypdf==6.10.2

# Optional: weasyprint provides an alternative HTML->PDF path but requires
# system libpango / libcairo. Listed for completeness; the project uses
# markdown_pdf instead.
weasyprint==68.1
